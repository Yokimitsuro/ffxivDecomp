Use the ffxiv-1x-server-decomp skill.

Analyze EXE function <FUNCTION_OR_ADDRESS> with Ghidra MCP.

Focus on whether it is related to networking, packet parsing, packet dispatch, Lua bridge, character select, world handoff, zone entry, player init, or actor spawn.

Write a finding under docs/re/exe/ and, if server behavior is implied, also write/update a docs/server/ requirement. Commit the result.
