Use the ffxiv-1x-server-decomp skill.

Analyze Lua file <LUA_FILE_PATH>.

Identify functions, events, native calls, state variables, loading/zone/actor/character references, and server-side implications.

If it is Lua bytecode, use unluac.jar from config/project.local.yml if needed.

Write a finding under docs/re/lua/ and commit the result.
